# 무료 라이브 완주자 전자책 자동 발송

라이브를 끝까지 본 사람에게만 "합격 비법 전자책"을 자동으로 주는 기능. 코드는 대표님이 라이브 마지막에 직접 공개하고, 참석자는 사이트에서 코드를 입력해 그 자리에서 바로 다운로드한다. **이메일 발송 없음 — 화면 다운로드만.**

## 대표님이 할 일 — 없음

- Supabase SQL 실행 ❌ (필요한 테이블 없음 — 기존에 이미 쓰던 `feedback` 테이블에 다운로드 로그만 조용히 남김)
- Supabase Storage에 PDF 업로드 ❌ (전자책 PDF는 저장소 코드(`ebooks/wonderfulcrew-ebook.pdf`)에 이미 포함되어 배포와 함께 자동으로 올라감)
- 관리자 페이지 로그인 ❌ (코드/파일/제목을 Supabase가 아니라 `api/_ebook-config.js` 파일 하나로 관리 — 대표님은 그냥 카톡으로 "다음 코드는 OOO로 해줘"라고만 알려주시면 됨)

## 지금 설정된 값

- 코드: `WCLIVE0920` (9월 20일 라이브용 기본값)
- 파일: `ebooks/wonderfulcrew-ebook.pdf` (제가 만든 초안 전자책, 별도로 PDF 전달해 드림)
- 참석자 페이지: `wonderfulcrew.com/ebook.html`

## 라이브 할 때마다 할 일 — 딱 하나

1. 라이브 마지막에 코드(`WCLIVE0920` 또는 다음에 정할 값)를 화면/채팅으로 공개.

다음 라이브 때 코드를 바꾸고 싶으면 그냥 저한테 새 코드를 알려주세요. 제가 `api/_ebook-config.js`만 고쳐서 바로 배포하면 끝입니다. 전자책 PDF를 새로 바꾸고 싶을 때도 파일만 주시면 제가 저장소에 반영해서 배포합니다.

## 참고 — 배포는 어떻게 되나요

이 기능은 지금 `claude/free-live-ebook-automation-xd171d` 브랜치에 있고, 실제 사이트에 반영되려면 이 브랜치가 `main`에 머지되어야 합니다. 지금 이 PR을 머지해 주시면 바로 `wonderfulcrew.com/ebook.html`이 살아있는 상태로 배포돼요. 이후 코드 변경(예: 다음 라이브 코드 교체)도 똑같이 PR을 만들고 머지하는 방식으로 갈지, 아니면 제가 이런 작은 설정 변경 건은 확인 후 바로 main에 반영해도 될지는 대표님 판단이 필요합니다.

## 홍보 반영 내역

- `apply.html`, `landing.html` — 신청 폼 위에 "라이브를 끝까지 보신 분께만 전자책을 드려요" 배너
- `api/seminar-signup.js` — 신청 완료 자동 메일에 같은 문구 한 줄 추가
- `blog-drafts.html` — 원고 25번으로 이 이벤트를 홍보하는 블로그 글 추가 (네이버 블로그 등에 그대로 발행 가능)

## 발급 현황 확인

Supabase 대시보드에서 `feedback` 테이블을 `type = '전자책 다운로드'`로 필터링하면 누가 언제 받아갔는지 볼 수 있어요. (별도 관리자 페이지 없음 — 필요하면 만들어 드릴게요.)
