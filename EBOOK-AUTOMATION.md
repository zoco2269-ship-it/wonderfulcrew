# 무료 라이브 완주자 전자책 자동 발송

라이브를 끝까지 본 사람에게만 "합격 비법 전자책"을 자동으로 주는 기능. 코드는 대표님이 라이브 마지막에 직접 공개하고, 참석자는 사이트에서 코드를 입력해 그 자리에서 바로 다운로드한다. **이메일 발송은 하지 않음** — 화면 다운로드만.

## 처음 한 번만 할 일

1. **Supabase 테이블 생성** — `supabase-schema.sql`의 9, 10번 항목(`ebook_config`, `ebook_unlocks`)을 Supabase SQL Editor에서 실행.
2. **환경변수 확인** — 이미 있는 값 그대로 재사용됨. 새로 추가할 건 없음.
   - `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` (DB)
   - `PUSH_ADMIN_KEY` (관리자 페이지 인증, 기본값 `wc-push-admin-2026`)

## 전자책 콘텐츠 — 초안 완성

`합격 비법 노트 — 원더풀크루가 실제로 하는 이야기` 8장 구성으로 초안 PDF를 만들어 대표님께 별도로 전달했습니다.

1. 서류에서 걸러지는 이유 — 자소서 체크포인트
2. 사진 한 장으로 갈리는 첫인상
3. 그루밍 디테일 — 심사위원이 3초 안에 보는 것들
4. 실전 기출 질문 10개 + 원더풀크루식 답변 구조("한 줄 공식")
5. 최종 면접에서 떨어지는 사람들의 공통점 5가지
6. 그룹 디스커션 — 면접관이 실제로 보는 것
7. 합격생들이 마지막에 바꾼 딱 한 가지
8. 라이브 다음 날부터 할 일 체크리스트

내용·톤을 검토하시고, 필요하면 문구를 다듬거나 실제 합격생 사례로 교체해 주세요. 확정되면 아래 순서대로 그대로 사용하시면 됩니다.

## 라이브 할 때마다 할 일

1. 전자책 PDF를 Supabase Storage `wonderfulcrew` 버킷(이미 공개 버킷)의 `ebooks/` 폴더에 업로드하고 공개 URL 복사.
2. `wonderfulcrew.com/admin-ebook.html` 접속 → 이번 라이브용 **코드**, **PDF 링크**, **제목** 입력 후 저장.
3. 라이브 진행 → 마지막에 코드를 화면/채팅으로 공개.
4. 참석자는 `wonderfulcrew.com/ebook.html`에서 이름/연락처/이메일/코드 입력 → 통과 시 그 자리에서 바로 다운로드 버튼 노출.
5. 다음 라이브 전에 코드만 새로 바꿔서 저장하면 이전 코드는 자동으로 무효화됨(항상 최신 1개만 유효).

## 발급 현황 확인

`admin-ebook.html`에서 "불러오기"를 누르면 지금까지 총 발급 건수가 보임. 개별 발급자 목록(이름/연락처/이메일)이 필요하면 Supabase 대시보드에서 `ebook_unlocks` 테이블을 조회.

## 홍보 반영 내역

- `apply.html`, `landing.html` — 신청 폼 위에 "라이브를 끝까지 보신 분께만 전자책을 드려요" 배너 추가
- `api/seminar-signup.js` — 신청 완료 자동 메일에 같은 문구 한 줄 추가 (신청 직후 완주 동기부여)
- `blog-drafts.html` — 원고 25번으로 이 이벤트를 홍보하는 블로그 글 추가 (네이버 블로그 등에 그대로 발행 가능)

## 아직 안 된 것

- 전자책 PDF를 Supabase Storage에 실제로 업로드하는 것 (초안 PDF를 검토·확정한 뒤 업로드하고 admin-ebook.html에 링크만 넣으면 끝)
- `seminar_signups`(무료 라이브 신청자 명단)와의 대조는 넣지 않음 — 코드 자체가 "라이브를 실제로 본 사람만 아는 정보"라 별도 명단 대조 없이도 스팸 방지가 됨. 필요해지면 추가 가능.
